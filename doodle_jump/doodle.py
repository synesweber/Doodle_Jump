import pygame

class Doodle:
    def __init__(self,):
        if pos is None:
            pos = [200, 600]
        self.img = pygame.transform.scale(pygame.image.load("./images/penguin.png"), (40, 40))
        self.pos = pos




